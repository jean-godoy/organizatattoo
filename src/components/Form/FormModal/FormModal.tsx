import { ReactNode } from 'react';
import './formModal.css';

interface TProps {
    children: ReactNode
}

export const FormModal = ({ children }: TProps) => {

    return (
        <form>
        
        
        </form>
    );
}